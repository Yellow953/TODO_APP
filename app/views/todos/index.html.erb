<div class="m-4 p-4">
  <h1>Todo List</h1>

  <%= link_to "New todo", new_todo_path, class:"btn btn-primary m-1" %>

  <div id="todos">
    <% @todos.each do |todo| %>
      <%= link_to (render todo), root_path, class:"text-black text-decoration-none" %>
    <% end %>
  </div>
</div>